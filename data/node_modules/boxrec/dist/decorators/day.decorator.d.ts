/**
 * Adds a getter to the class that returns the day column and parses it
 * @constructor
 */
export declare function DayGetter(): (target: any) => void;
export interface DayInterface {
    readonly day: string;
}
