/**
 * Adds a getter to the class that returns the rating for this bout
 * @constructor
 */
export declare function RatingGetter(): (target: any) => void;
export interface RatingInterface {
    readonly rating: number | null;
}
