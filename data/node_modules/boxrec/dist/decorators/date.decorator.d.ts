/**
 * Adds a getter to the class that returns the date column data
 * @constructor
 */
export declare function DateGetter(): (target: any) => void;
export interface DateInterface {
    readonly date: string;
}
