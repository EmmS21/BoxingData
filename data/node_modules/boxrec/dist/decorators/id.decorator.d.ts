import { BoxrecCommonTableHeader } from '../helpers';
/**
 * Adds a getter to the class that returns the id for that table row
 * @constructor
 */
export declare function IdGetter(commonId?: BoxrecCommonTableHeader.name | BoxrecCommonTableHeader.links): (target: any) => void;
export interface IdInterface {
    readonly id: number;
}
