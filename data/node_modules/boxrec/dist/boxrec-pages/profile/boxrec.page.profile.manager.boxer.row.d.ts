import { DivisionInterface } from '../../decorators/division.decorator';
import { BoxrecLocation, Record, Stance, WinLossDraw } from '../boxrec.constants';
import { WeightDivision } from '../champions/boxrec.champions.constants';
export declare class BoxrecPageProfileManagerBoxerRow implements DivisionInterface {
    private headerColumns;
    division: WeightDivision | null;
    private readonly $;
    constructor(headerColumns: string[], boxrecBodyString: string);
    get age(): number | null;
    get debut(): string | null;
    get last6(): WinLossDraw[];
    get name(): string;
    get record(): Record;
    get residence(): BoxrecLocation;
    get stance(): Stance | null;
}
