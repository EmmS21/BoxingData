import { DateInterface } from '../../decorators/date.decorator';
import { DayInterface } from '../../decorators/day.decorator';
import { OutputInterface } from '../../decorators/output.decorator';
import { BoxrecLocation } from '../boxrec.constants';
import { BoxrecPageVenueEventsRowOutput } from './boxrec.page.venue.constants';
export declare class BoxrecPageVenueEventsRow implements DateInterface, DayInterface, OutputInterface {
    private headerColumns;
    date: string;
    day: string;
    id: number | null;
    output: BoxrecPageVenueEventsRowOutput;
    private readonly $;
    constructor(headerColumns: string[], boxrecBodyBout: string);
    get location(): BoxrecLocation;
}
