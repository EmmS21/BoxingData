import { BoxrecFighterRole } from 'boxrec-requests';
import { DivisionInterface } from '../../decorators/division.decorator';
import { IdInterface } from '../../decorators/id.decorator';
import { OutputInterface } from '../../decorators/output.decorator';
import { BoxrecLocation, Record, WinLossDraw } from '../boxrec.constants';
import { WeightDivision } from '../champions/boxrec.champions.constants';
import { BoxrecPageSearchRowOutput } from './boxrec.search.constants';
export declare class BoxrecPageSearchRow implements DivisionInterface, IdInterface, OutputInterface {
    private headerColumns;
    division: WeightDivision | null;
    id: number;
    output: BoxrecPageSearchRowOutput;
    private readonly $;
    constructor(headerColumns: string[], boxrecBodySearchRow: string);
    get alias(): string | null;
    get career(): Array<number | null>;
    get last6(): WinLossDraw[];
    get name(): string | null;
    get record(): Record;
    get residence(): BoxrecLocation;
    get sport(): BoxrecFighterRole;
}
