/// <reference types="cheerio" />
import { BoxrecBasic } from '../../boxrec.constants';
export declare abstract class BoxrecPageEventCommonRow {
    protected headerColumns: string[];
    protected readonly $: CheerioStatic;
    constructor(headerColumns: string[], boxrecBodyBout: string, additionalData?: string | null);
    get venue(): BoxrecBasic;
}
