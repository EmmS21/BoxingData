/// <reference types="cheerio" />
import { IdInterface } from '../../decorators/id.decorator';
import { BoxrecLocation, Record, Stance, WinLossDraw } from '../boxrec.constants';
export declare abstract class BoxrecPageRatingsRow implements IdInterface {
    protected headerColumns: string[];
    id: number;
    protected readonly $: CheerioStatic;
    constructor(headerColumns: string[], boxrecBodyBout: string);
    get hasBoutScheduled(): boolean;
    get last6(): WinLossDraw[];
    get name(): string;
    get points(): number | null;
    get record(): Record;
    get residence(): BoxrecLocation;
    get stance(): Stance;
}
