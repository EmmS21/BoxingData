import { RequestInit, Response } from "node-fetch";
/**
 * Takes a person's profile and returns what kind of profile role it is
 * @param html
 */
export declare const getRoleOfHTML: (html: string) => string | null;
export declare function requestWrapperFullResponse(url: string, cookies?: string, parametersOrQueryString?: any): Promise<Response>;
/**
 * This acts as a middleware between the package and the requests to BoxRec
 * For example if we hit a 429 "Too Many Requests" we want to return a proper message to the callers
 */
export declare function requestWrapper(url: string, cookies?: string, parametersOrQueryString?: RequestInit | any): Promise<string>;
